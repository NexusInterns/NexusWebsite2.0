// // import * as Components from './index.tsx';
// import axios from 'axios';
// import { useEffect, useState } from "react";

function App()
{
  // let items = ["A", "B", "C", "D", "E", "F", "G"];

  // const handleSelectedItem = (item: string) => console.log(item);

  // const [subscriberList, setSubscriberList] = useState([]);

  // useEffect(() => {
  //   axios.get("http://localhost:5172/subscribers").then((response) => {
  //     setSubscriberList(response.data);
  //   })
  // }, []);

  return (
    <>
    Hello World
      {/* <Components.Alert show> Hello World </Components.Alert>
      <Components.ListGroup items={items} heading="Choices" onSelectItem={handleSelectedItem} />
      <Components.Button color="primary" onClick={() => console.log(true)}> Press </Components.Button> */}
    </>
  );
}

export default App
