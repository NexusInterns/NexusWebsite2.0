import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nexuswebsite2.0',
  appName: 'nexuswebsite2-0',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
